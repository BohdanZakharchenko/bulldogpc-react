import React from "react";

const ChatToggleButton = ({ onClick }) => {
  return (
    <button className="support-chat-toggle" onClick={onClick}>
      💬
    </button>
  );
};

export default ChatToggleButton;
